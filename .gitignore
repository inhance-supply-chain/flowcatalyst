# Rust build artifacts
/target/
**/*.rs.bk

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Environment files
.env
.env.local
.env.*.local
.env.production

# JWT keys (auto-generated)
.jwt-keys/

# Node modules (for npm scripts)
node_modules/
package-lock.json

# Local development databases
*.db
*.sqlite
*.sqlite3

# Log files
*.log
logs/

# Test coverage
coverage/
*.profraw
*.profdata

# Docker volumes / local data
docker-data/
localstack-data/

# Claude Code harness — runtime state, not shared.
# settings.json IS tracked (project-shared); the lock + local override are not.
.claude/scheduled_tasks.lock
.claude/settings.local.json

# LocalStack
.localstack/
/clients/typescript-sdk/dist
/clients/typescript-sdk/dist
/clients/typescript-sdk/dist
